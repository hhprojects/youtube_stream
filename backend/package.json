{
  "name": "youtube-streamer-backend",
  "version": "1.0.0",
  "description": "YouTube Streamer backend — Express + yt-dlp. Searches YouTube, downloads audio as m4a, and serves the files for the native Android client.",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check:discovery": "node scripts/check-discovery.js",
    "check:podcasts": "node scripts/check-podcasts.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.0"
  }
}
